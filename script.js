const nav=document.querySelector('.nav'),menu=document.querySelector('.menu');menu?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const profile=document.querySelector('input[name="perfil"]'),choices=[...document.querySelectorAll('.choice button')];function setProfile(v){profile.value=v;choices.forEach(b=>b.classList.toggle('active',b.dataset.value===v))}document.querySelectorAll('[data-profile]').forEach(el=>el.addEventListener('click',()=>setProfile(el.dataset.profile)));choices.forEach(b=>b.addEventListener('click',()=>setProfile(b.dataset.value)));document.querySelector('#leadForm')?.addEventListener('submit',e=>{e.preventDefault();document.querySelector('#formMsg').textContent='Demonstração: seus dados não foram enviados ou salvos. O canal comercial ainda não está conectado.'});
